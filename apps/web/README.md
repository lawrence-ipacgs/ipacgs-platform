# Web (not started)

Epic 0 has no UI tickets — the portal work (React/Next.js, per the
architecture document's Layer technology table) starts once there's an API
worth putting a screen in front of. Placeholder directory so the repo layout
matches `README.md`'s description from day one, rather than getting added
later as an afterthought.
